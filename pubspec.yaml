name: moodist_mot
description: "Moodist MOT"
version: 0.0.1
homepage:

environment:
  sdk: '>=3.2.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # state
  flutter_bloc: ^8.1.3

  # network
  dio: ^5.4.0

  # service
  logger: ^2.2.0
  url_launcher: ^6.2.4
  flutter_pdfview: ^1.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
